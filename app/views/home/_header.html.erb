<nav class="navbar navbar-expand-lg mt-4 ml-4">
  <div class="container-fluid">
    <%= link_to "HELEN'S<br>WARDROBE".html_safe, root_path, class: "navbar-brand logo-box" %> 

    <div class="collapse navbar-collapse d-flex justify-content-between" id="navbarNav">
      <ul class="navbar-nav mb-2 mb-lg-0 ml-4"> 
        <li class="nav-item px-2">
          <%= link_to "HOME", root_path, class: "nav-link text-uppercase #{'active' if current_page?(root_path)}" %>
        </li>
        <li class="nav-item px-2">
          <%= link_to "OUTFITS", outfits_path, class: "nav-link text-uppercase #{'active' if current_page?(outfits_path)}" %>
        </li>
        <li class="nav-item px-2">
          <%= link_to "ADD", new_outfit_path, class: "nav-link text-uppercase #{'active' if current_page?(new_outfit_path)}" %>
        </li>
      </ul>

      <%= form_with url: outfits_path, method: :get, class: "d-flex" do |f| %>
        <%= f.text_field :query, 
            class: "form-control me-2", 
            placeholder: "Search...",
            value: params[:query] %>
        <%= f.submit "SEARCH", class: "btn btn-outline-secondary text-uppercase" %> 
      <% end %>
    </div>
  </div>
</nav>

<style>
  .logo-box {
    border: 2px solid; 
    padding: 33px 15px; 
    text-align: center;
    /* Add font styles here */
  }

  .nav-link.active {
    text-decoration: underline;
    background-color: transparent !important; 
    color: inherit !important; /* Use parent color */
  }

  .navbar {
    /* font-family: 'YourSerifFont', serif; */ /* Example font */
  }

  /* Make navbar brand link black */
  .navbar-brand,
  .navbar-brand:hover {
    color: black !important; 
  }

  /* Make nav links black */
  .nav-link {
    color: black !important; 
  }

  /* Keep nav links black on hover (unless it's the active one being underlined) */
  .nav-link:not(.active):hover {
     color: black !important; 
  }
</style> 
